# Salesforce Ticket Template

## Project

- Project name: `{{PROJECT_NAME}}`
- Team: `{{TEAM_NAME}}`

## Ticket ID

`[SF-0000]`

## Business context

Describe why this change is needed and expected business value.

## User story

As a `[user/persona]`, I want `[capability]`, so that `[business outcome]`.

## Acceptance criteria

- Given `[precondition]`, when `[action]`, then `[expected result]`.
- Given `[precondition]`, when `[action]`, then `[expected result]`.

## Technical constraints

- Dev org alias: `{{DEV_ORG_ALIAS}}`
- UAT enabled: `{{HAS_UAT}}` (`{{UAT_ORG_ALIAS}}`)
- Prod enabled: `{{HAS_PROD}}` (`{{PROD_ORG_ALIAS}}`)
- Deployment policy: `{{DEPLOYMENT_POLICY}}`

## Impacted metadata (to verify)

- Objects:
- Fields:
- Apex:
- LWC:
- Flow:
- Security artifacts:

## Out of scope

- Explicit exclusions.

## Open questions

- Question:
- Owner:

## Definition of done reference

`docs/definition-of-done.md`
